# SEO helpers

- `ao.shared.seo.product_ld(product)` returns JSON-LD string for product (name, description, sku, price, availability).
- `ao.shared.seo.page_ld(page)` returns JSON-LD for generic page.
- Not wired by default; call from resolver when rendering pages/products.
