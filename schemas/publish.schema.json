{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Publish",
  "type": "object",
  "required": ["publishId", "versionId", "manifestTx"],
  "properties": {
    "publishId": {"type": "string", "minLength": 1, "maxLength": 128},
    "versionId": {"type": "string", "minLength": 1, "maxLength": 128},
    "manifestTx": {"type": "string", "minLength": 1, "maxLength": 128},
    "activatedAt": {"type": "string", "format": "date-time"},
    "rollbackTo": {"type": "string", "minLength": 1, "maxLength": 128}
  }
}
