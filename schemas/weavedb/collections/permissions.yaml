format: weavedb-collection/v1
name: permissions
schema:
  type: object
  required: [id]
  properties:
    id:    { type: string }
    name:  { type: string }
    scope: { type: string }
indexes:
  - fields: [id]
