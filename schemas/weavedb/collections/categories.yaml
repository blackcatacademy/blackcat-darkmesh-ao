format: weavedb-collection/v1
name: categories
schema:
  type: object
  required: [id, site_id, name]
  properties:
    id:      { type: string }
    site_id: { type: string }
    name:    { type: string }
    parent:  { type: string }
indexes:
  - fields: [site_id, name]
