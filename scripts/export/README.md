# Export scripts

Current tools:
- `audit_dump.lua [N]` — prints last N lines (default 50) from the audit log (`AUDIT_LOG_DIR` or default `arweave/manifests`).

Usage:
```bash
lua scripts/export/audit_dump.lua 50
```
